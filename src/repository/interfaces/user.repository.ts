/**
 * IUserRepository — contract for user data operations.
 *
 * Re-exported from the canonical definition kept in
 * `src/data/repositories/interfaces/user.repository.ts` so that both the
 * old and new import paths resolve to the same interface.
 */
export type { IUserRepository } from '@/data/repositories/interfaces/user.repository'
